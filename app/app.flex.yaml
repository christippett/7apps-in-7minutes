service: flexible
runtime: custom
env: flex

manual_scaling:
  instances: 1

resources:
  cpu: 1
  memory_gb: 0.9
  disk_size_gb: 10

network:
  name: demo-vpc
  subnetwork_name: demo-vpc-subnet-au-se1

liveness_check:
  path: "/"

readiness_check:
  path: "/"
